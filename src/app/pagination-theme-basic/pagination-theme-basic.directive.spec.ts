import { PaginationThemeBasicDirective } from './pagination-theme-basic.directive';

describe('PaginationThemeBasicDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginationThemeBasicDirective();
    expect(directive).toBeTruthy();
  });
});

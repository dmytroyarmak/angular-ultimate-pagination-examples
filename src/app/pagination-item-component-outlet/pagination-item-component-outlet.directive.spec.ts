import { PaginationItemComponentOutletDirective } from './pagination-item-component-outlet.directive';

describe('PaginationItemComponentOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginationItemComponentOutletDirective();
    expect(directive).toBeTruthy();
  });
});
